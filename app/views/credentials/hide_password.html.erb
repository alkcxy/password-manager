<% frame_id = params[:mobile].present? ? "password_m_#{@credential.id}" : "password_#{@credential.id}" %>
<%= turbo_frame_tag frame_id do %>
  <%= link_to reveal_password_credential_path(@credential, mobile: params[:mobile]), class: "btn btn-sm btn-outline-secondary", title: "Mostra", aria: { label: "Mostra" } do %><i class="bi bi-eye"></i><% end %>
<% end %>
